% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/random_variable.R
\name{random_variable}
\alias{random_variable}
\alias{print,ripr::random_variable-method}
\title{A random variable on a sample space}
\usage{
random_variable(
  f,
  family,
  label = "<rv>",
  op = NA_character_,
  operands = list()
)

## S7 method for class <ripr::random_variable>
print(x, ...)
}
\arguments{
\item{f}{The mapping that defines the random variable, accepting an \verb{(n, d)}
matrix and returning \code{n} numbers.}

\item{family}{The \link{sampling_family} whose sample space this is defined on.}

\item{label}{How to name this variable when printing. Ignored when \code{op} is
given, since the expression is then built from the operands.}

\item{op}{The operator that produced this variable, or \code{NA} for a leaf. Set
by \link{random_variable_arithmetic}; there is rarely a reason to pass it.}

\item{x}{A \link{random_variable}.}

\item{...}{Ignored.}
}
\value{
A callable \code{random_variable}.
}
\description{
A \code{random_variable} \code{X} takes one element of the sample space (a length-\code{d}
vector for a \code{d}-dimensional sample space) and returns a number, or \code{n}
elements as an \verb{(n, d)} matrix and returns \code{n} numbers. The input is checked
by \code{\link[=as_outcomes]{as_outcomes()}} first.
}
\details{
\code{Inf} can be a legitimate value here. A likelihood ratio can be genuinely
infinite when the denominator is not absolutely continuous with respect to
the numerator.
}
\examples{
X <- random_variable(\(x) dnorm(x, 1), family = gaussian_family(dim = 1))
X(as.matrix(0:2))
}
\seealso{
\code{\link[=mixture_likelihood]{mixture_likelihood()}}, \link{random_variable_arithmetic}
}
