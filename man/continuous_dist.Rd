% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixing.R
\name{continuous_dist}
\alias{continuous_dist}
\title{Continuous distributions}
\usage{
continuous_dist(sample_space = space())
}
\arguments{
\item{sample_space}{The \link{space} this is a law over. Inherited from
\link{distribution}.}
}
\description{
A \link{distribution} that is continuous rather than discrete, so it has a
density whereas \link{finite_dist}s have atoms and weights. See \code{\link[=gaussian_dist]{gaussian_dist()}},
\code{\link[=dirichlet]{dirichlet()}} and \code{\link[=truncated_dirichlet]{truncated_dirichlet()}}.
}
\examples{
# `continuous_dist` is abstract; dirichlet() subclasses it, e.g.
S7::S7_inherits(dirichlet(alpha = c(2, 1, 1)), continuous_dist)
n_atoms(dirichlet(alpha = c(2, 1, 1)))
}
