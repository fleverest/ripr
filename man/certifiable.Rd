% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/oracle_bound.R
\name{certifiable}
\alias{certifiable}
\title{Can \verb{sup_theta G} be bounded deterministically over this face?}
\usage{
certifiable(face, engine, family, control = bnb_control())
}
\arguments{
\item{face}{A \code{face}.}

\item{engine}{An \code{expectation_engine}.}

\item{family}{A \code{family}.}

\item{control}{A \code{\link[=bnb_control]{bnb_control()}} list, consulted for \code{max_coef}.}
}
\value{
\code{TRUE} or \code{FALSE}.
}
\description{
A plain predicate rather than a generic: it must be cheap, total, and
testable, and it has to consult three objects at once. Including the cost
budget lets callers plan rather than discover failure mid-run.
}
\details{
All four conditions are necessary. Being exact is not sufficient -- the
Bernstein identity is a property of the \emph{multinomial} pmf, so
\code{\link[=bernstein_form]{bernstein_form()}} is consulted on the family, not the engine. The face must
be a bounded simplex inside the probability simplex, since that is what the
reparametrisation subdivides.
}
