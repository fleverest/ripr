% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{em_step}
\alias{em_step}
\title{EM sweep}
\usage{
em_step(state, times = 1L, record_gap = FALSE, until = NULL)
}
\arguments{
\item{state}{A \link{ripr_state}.}

\item{times}{Steps to take.}

\item{record_gap}{Sweep the Frank--Wolfe oracle over the new mixture that the
sweep \emph{produced}, filling \code{gap} and \code{gap_theta}. Off by default, since it
costs a full oracle sweep per row.}

\item{until}{Optional predicate; see \link{predicates}.}
}
\value{
The updated \link{ripr_state}.
}
\description{
Each sweep updates every weight, then moves every atom within its own
subnull. The support neither grows nor shrinks: only an oracle method such as
\code{\link[=fw_step]{fw_step()}} or \code{\link[=lb_step]{lb_step()}} can add an atom to the mixture.
}
\examples{
set.seed(1)
fam <- multinomial_family(n_trials = 4L, k = 3L)
plurality <- null_model(
  fam,
  list(
    simplex_region(vertices = cbind(c(0.5, 0.5, 0), c(0, 1, 0), c(0, 0, 1))),
    simplex_region(vertices = cbind(c(0.5, 0, 0.5), c(0, 1, 0), c(0, 0, 1)))
  )
)
Q <- fam(c(0.4, 0.35, 0.25))
state <- ripr_init(Q, plurality) |> fw_step(times = 3L) |> em_step(times = 10L)
state@trace$kl
}
\seealso{
\link{oracles}, \link{predicates}
}
