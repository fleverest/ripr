% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distribution.R
\name{distribution}
\alias{distribution}
\title{Probability distribution (abstract root)}
\usage{
distribution()
}
\description{
The umbrella for every probability law the package handles. It has two
branches: a \link{mixing} measure over a family's \emph{parameter} space Theta, and an
\link{outcome_distribution} over its \emph{sample} space X. The two are bridged by
\code{\link[=as_marginal]{as_marginal()}}, which pushes a mixing through a family's kernel to obtain the
induced outcome distribution (a \link{marginal}).
}
\details{
This root carries no interface of its own -- it exists so the two branches
share a common type. The evaluable interface (\code{dist_log_density},
\code{dist_sample}) lives on \link{outcome_distribution}, the sample-space branch the
RIPr problem actually consumes.
}
