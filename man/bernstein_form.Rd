% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family.R
\name{bernstein_form}
\alias{bernstein_form}
\title{Is \code{p_theta(x)} a Bernstein basis function of \code{theta}?}
\usage{
bernstein_form(family)
}
\arguments{
\item{family}{A \code{family}.}
}
\value{
\code{NULL}, or \code{list(n = , K = , tally = )}.
}
\description{
The capability behind the deterministic certified bound (see
\code{\link[=oracle_bound]{oracle_bound()}}). Contract: return \code{NULL} when the family's density is not a
simplicial Bernstein basis function of its parameter, and otherwise
\code{list(n = , K = , tally = )} where \code{n} is the Bernstein degree, \code{K} the
number of barycentric coordinates (so \code{theta} ranges over the \code{(K-1)}-simplex
and \code{param_dim()} is \code{K}), and \code{tally} the \verb{(M, K)} matrix of exponent
multi-indices \strong{in the same row order as the family's \code{\link[=support]{support()}}} -- and
hence as \code{engine@outcomes} for an \code{\link[=exact_engine]{exact_engine()}} over that family.
}
\details{
The capability belongs to the family, not the engine: being exact is
necessary but not sufficient. It is the \emph{multinomial} pmf that happens to be
the degree-\code{n} Bernstein basis function at count vector \code{x}, so an exact
engine over some other finite-support family does not qualify. Families
without a method inherit the \code{NULL} default and degrade to the heuristic
oracle rather than being silently certified.
}
\keyword{internal}
