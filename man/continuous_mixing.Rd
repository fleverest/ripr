% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixing_measure.R
\name{continuous_mixing}
\alias{continuous_mixing}
\title{Mixing measures with no atoms}
\usage{
continuous_mixing()
}
\description{
A law over the parameter space that is continuous rather than discrete, so
it has a density where a \link{finite_mixing} has weights. \code{\link[=dirichlet_mixing]{dirichlet_mixing()}}
and \code{\link[=truncated_dirichlet]{truncated_dirichlet()}} are of this kind, as is \code{\link[=gaussian_mixing]{gaussian_mixing()}}.
}
\details{
The three generics a discrete measure answers by listing its support --
\code{\link[=n_atoms]{n_atoms()}}, \code{\link[=atoms]{atoms()}} and \code{weights()} -- have no answer here, so \code{n_atoms()}
is \code{NA} and the other two error. What a continuous measure offers instead is
\code{\link[=draw_theta]{draw_theta()}}, and the pairing with a family still has to supply a density
for the induced mixture; see \code{\link[=induced_distribution]{induced_distribution()}}.
}
\examples{
# `continuous_mixing` is abstract; dirichlet_mixing() subclasses it, e.g.
S7::S7_inherits(dirichlet_mixing(alpha = c(2, 1, 1)), continuous_mixing)
n_atoms(dirichlet_mixing(alpha = c(2, 1, 1)))
}
