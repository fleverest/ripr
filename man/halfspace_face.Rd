% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/faces.R
\name{halfspace_face}
\alias{halfspace_face}
\title{Half-space face \verb{\{theta : <v, theta> <= c\}}}
\usage{
halfspace_face(v, c, face_index = NA_real_)
}
\arguments{
\item{v}{Normal vector of the constraint.}

\item{c}{Offset: the face is \verb{\{theta : <v, theta> <= c\}}.}

\item{face_index}{Integer label carried through results.}
}
\value{
A \code{halfspace_face}.
}
\description{
The Gaussian null building block. Projection and membership are closed form.
The face is parametrised over its full interior by
\verb{theta(z, s) = anchor + B z - softplus(s) * v / ||v||}, where \code{anchor} is the
boundary projection of the origin, \code{B} an orthonormal basis of the boundary
hyperplane, and \code{softplus(s) >= 0} the distance into the half-space; the
coordinate vector is \code{c(z, s)}. The oracle runs BFGS over these coordinates
from Gaussian random seeds biased toward the boundary.
}
