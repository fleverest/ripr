% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distribution.R
\name{induced_distribution}
\alias{induced_distribution}
\alias{print,ripr::induced_distribution-method}
\alias{format,ripr::induced_distribution-method}
\title{The distribution \code{P_W} induced by a mixing measure \code{W} and a family}
\usage{
induced_distribution(family, mixing)
}
\arguments{
\item{family}{The \link{parametric_family} whose kernel is pushed forward.}

\item{mixing}{A \link{distribution} over its parameter space, or a parameter
vector for a point mass.}
}
\value{
An \code{induced_distribution}.
}
\description{
The law of \code{X} when \eqn{\theta \sim W}{theta ~ W} and
\eqn{X \sim P_\theta}{X ~ P_theta}. Both the alternative
\eqn{Q = P_{W_1}}{Q = P_W1} and the fitted
\eqn{\widehat{P}^* = P_{\widehat{W}_0}}{P_star_hat} are of this form.

\code{format()} names the family and how it was mixed, distinguishing
a point mass from a genuine mixture.
}
\details{
A kernel extends canonically from points to measures, so the degenerate case
is not a separate type: a bare parameter vector is taken as a point mass, and
\code{induced_distribution(fam, theta)} is the family at \code{theta}. Calling a family
directly is the shorthand for both, and usually reads better:
\code{fam(theta)} and \code{fam(W)}.
}
\examples{
fam <- multinomial_family(n_trials = 4L, k = 3L)
induced_distribution(fam, c(0.5, 0.3, 0.2))
induced_distribution(fam, finite_dist(
  components = cbind(c(0.6, 0.2, 0.2), c(0.2, 0.6, 0.2)),
  weights = c(0.5, 0.5)
))
}
