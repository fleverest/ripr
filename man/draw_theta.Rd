% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixing_measure.R, R/dirichlet.R
\name{draw_theta}
\alias{draw_theta}
\alias{draw_theta,ripr::truncated_dirichlet-method}
\title{Draw parameters from a continuous mixing measure}
\usage{
draw_theta(mixing, n)
}
\arguments{
\item{mixing}{A \link{continuous_mixing}.}

\item{n}{Number of draws.}
}
\value{
\verb{(d, n)} numeric matrix, one parameter per column.
}
\description{
The sampling half of a \link{continuous_mixing}: \code{n} draws of
\eqn{\theta \sim W}{theta ~ W}. Columns, as parameters are everywhere else
in the package, so the result feeds \code{\link[=kernel_draw]{kernel_draw()}} a column at a time.

Rejection sampling from \code{Dir(alpha)}, keeping the proposals the
region contains.
}
\examples{
set.seed(1)
draw_theta(dirichlet_mixing(alpha = c(4, 3, 2)), n = 3L)
}
