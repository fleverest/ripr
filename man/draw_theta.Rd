% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixing_measure.R, R/dirichlet.R
\name{draw_theta}
\alias{draw_theta}
\alias{draw_theta,ripr::point_mixing-method}
\alias{draw_theta,ripr::finite_mixing-method}
\alias{draw_theta,ripr::truncated_dirichlet-method}
\title{Draw parameters from a mixing measure}
\usage{
draw_theta(mixing, n)
}
\arguments{
\item{mixing}{A \link{mixing_measure}.}

\item{n}{Number of draws.}
}
\value{
\verb{(d, n)} numeric matrix, one parameter per column.
}
\description{
\code{n} draws of \eqn{\theta \sim W}{theta ~ W}, for any \link{mixing_measure} W.

A point mass draws the same parameter every time.

A finite mixture draws its atoms with probability equal to their
weights.

Rejection sampling from \code{Dir(alpha)}, keeping the proposals the
region contains.
}
\details{
Yields parameter draws across columns, so the result feeds straight into
\code{\link[=kernel_draw]{kernel_draw()}}, whose matrix form draws one observation per column in a
single call.
}
\examples{
set.seed(1)
draw_theta(dirichlet_mixing(alpha = c(4, 3, 2)), n = 3L)
}
