% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quadrature.R
\name{resolve_engine}
\alias{resolve_engine}
\title{Resolve an engine spec against an alternative and a family}
\usage{
resolve_engine(spec, alternative, family, tol = 1e-08)
}
\arguments{
\item{spec}{An engine spec, e.g. from \code{\link[=exact_engine]{exact_engine()}} or \code{\link[=mc_engine]{mc_engine()}}.}

\item{alternative}{The alternative \eqn{Q}{Q}, an \link{distribution}.}

\item{family}{A \link{parametric_family}.}

\item{tol}{Tolerance on the weight sum.}
}
\value{
A \link{quadrature}.
}
\description{
Checks that the resulting rule integrates to one. That identity is what makes
\eqn{\sum_j w_j G(\theta_j) = 1}{sum_j w_j G(theta_j) = 1} hold for any
mixture, and hence what forces the duality gap to be non-negative, so a
violation is an error rather than a warning: every downstream quantity would
be wrong.
}
\details{
You need this only if you are writing a fit loop or an engine spec of your
own; in normal use you hand the spec to \code{\link[=ripr_init]{ripr_init()}}, which calls this for
you. The weight-sum check here is the contract every spec must satisfy.
}
\examples{
fam <- multinomial_family(n_trials = 3L, k = 2L)
Q <- mixture(fam, dirac(c(0.5, 0.5)))
resolve_engine(exact_engine(), Q, fam)
}
