% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/region.R
\name{h_region}
\alias{h_region}
\title{A convex polyhedron given by its half-space description}
\usage{
h_region(a, b, eq = FALSE)
}
\arguments{
\item{a}{\verb{(m, d)} numeric matrix of facet normals, one constraint per row.}

\item{b}{Numeric right-hand side, length \code{m}.}

\item{eq}{Logical, length \code{m} or recycled; \code{TRUE} marks an equality row.}
}
\value{
A \code{\link[=polyhedron_region]{polyhedron_region()}}.
}
\description{
The dual constructor to \code{\link[=polyhedron_region]{polyhedron_region()}}: the set
\code{{theta : a \%*\% theta <= b}}, with \code{eq} flagging rows that hold with
equality (e.g. \code{sum(theta) == 1}). The generators are derived by one exact
double-description step, and the rows given here are kept as the region's
facets exactly as declared.
}
\examples{
# The unit square in R^2:
h_region(a = rbind(diag(2), -diag(2)), b = c(1, 1, 0, 0))

# The halfspace `{theta_1 <= theta_2}`:
h_region(a = matrix(c(1, -1), nrow = 1), b = 0)
}
