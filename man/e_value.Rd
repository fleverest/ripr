% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/e_variable.R
\name{e_value}
\alias{e_value}
\title{Evaluate an e-variable on outcomes}
\usage{
e_value(ev, x, log = FALSE, corrected = TRUE)
}
\arguments{
\item{ev}{An \code{e_variable}.}

\item{x}{Outcomes: an \verb{(N, d)} matrix, or a length-d vector for a single
outcome.}

\item{log}{Return the log e-value instead? Default \code{FALSE}.}

\item{corrected}{Apply the \code{1 / (1 + gap)} rescaling that makes the e-variable
valid under the null? Default \code{TRUE}. With \code{FALSE} the raw likelihood ratio
\code{Q(x) / P*(x)} (the e-variable before the duality-gap correction) is
returned.}
}
\value{
Numeric vector of (log) e-values, one per outcome.
}
\description{
Computes the e-value \code{e(x) = (Q(x) / P*(x)) / (1 + gap)} at each outcome \code{x}.
}
