% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quadrature.R
\name{quadrature}
\alias{quadrature}
\title{Quadrature rule for expectations under the alternative}
\usage{
quadrature(
  nodes = NULL,
  log_w = integer(0),
  log_q = integer(0),
  family = parametric_family(),
  deterministic = logical(0)
)
}
\arguments{
\item{nodes}{\verb{(M, K)} matrix of evaluation points.}

\item{log_w}{Length-\code{M} log quadrature weights; \code{exp(log_w)} sums to 1.}

\item{log_q}{Length-\code{M} log density of the alternative at \code{nodes}.}

\item{family}{The \link{parametric_family} the nodes live over.}

\item{deterministic}{Is the rule free of sampling error?}
}
\value{
A \code{quadrature}.
}
\description{
Every quantity the optimiser touches is an expectation under \eqn{Q}{Q}: the
objective \eqn{KL(Q \| P_W)}{KL(Q || P_W)}, the linear oracle
\eqn{G(\theta) = E_Q[p_\theta / P_W]}{G(theta) = E_Q[p_theta / P_W]}, the EM
responsibilities, and the gap. So an engine is one thing: a set of nodes with
weights approximating \eqn{E_Q[\cdot]}{E_Q[.]}. Exact enumeration, Monte
Carlo, and deterministic quadrature differ only in how the nodes and weights
are chosen.
}
\details{
\code{log_q} is held separately from \code{log_w} because the two coincide only for the
exact engine. Under Monte Carlo the weights are \code{1/M} while \code{log_q} is the
density at the drawn nodes, and the objective needs both.

Build one with \code{\link[=resolve_engine]{resolve_engine()}} rather than the raw constructor. You need
either only if you are writing a fit loop or an engine spec of your own; in
normal use you pass a spec such as \code{\link[=exact_engine]{exact_engine()}} to \code{\link[=ripr_init]{ripr_init()}}, which
resolves it and hands the engine to the step verbs.
}
\examples{
fam <- multinomial_family(n_trials = 3L, k = 2L)
Q <- mixture(fam, dirac(c(0.5, 0.5)))
resolve_engine(exact_engine(), Q, fam)
}
