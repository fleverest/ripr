% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unconstrained_region.R
\name{unconstrained_region}
\alias{unconstrained_region}
\title{The whole of \code{R^d} as a region}
\usage{
unconstrained_region(d)
}
\arguments{
\item{d}{Integer dimension.}
}
\value{
An \code{unconstrained_region}.
}
\description{
The unconstrained case: \eqn{\Theta = \mathbb{R}^d}{Theta = R^d}, with the
identity chart. This is the parameter space of a \code{\link[=gaussian_family]{gaussian_family()}}, and the
way to say that a null places no constraint at all.
}
\details{
Being unbounded it has no vertices, so like \code{\link[=halfspace_region]{halfspace_region()}} it admits no
certified gap bound.

The name says what the region is, rather than what it is made of: a
\link{real_space} is a \emph{sample} space, and the two were too easy to confuse while
this one carried the same \code{real_} prefix.
}
\examples{
unconstrained_region(2L)
project(unconstrained_region(2L), c(3, -1))
}
