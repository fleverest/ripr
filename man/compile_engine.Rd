% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quadrature.R
\name{compile_engine}
\alias{compile_engine}
\title{Compile the family's log-likelihood at the engine's nodes}
\usage{
compile_engine(engine)
}
\arguments{
\item{engine}{A \link{quadrature}.}
}
\value{
A function of \code{theta_mat}.
}
\description{
Returns a function of \code{theta_mat} giving the \verb{(M, C)} matrix of log densities
at the nodes. Compile once at the top of a step and reuse: the nodes do not
move, so recompiling per evaluation repeats work that depends on them alone.
}
\details{
Engines hold their nodes as plain data and compile on demand rather than
storing the evaluator, so a serialised engine carries no captured
environment.
}
