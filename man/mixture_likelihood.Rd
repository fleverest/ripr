% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/random_variable.R
\name{mixture_likelihood}
\alias{mixture_likelihood}
\title{The mixture likelihood, as a random variable}
\usage{
mixture_likelihood(dist, label = NULL)
}
\arguments{
\item{dist}{An \link{outcome_distribution}.}

\item{label}{How to name it when printing. \code{NULL} takes how the argument was
written, so \code{mixture_likelihood(Q)} prints as \code{Q}. This degenerates when
the call is behind a helper or inside a loop, since it records how the
variable was written rather than what it is. Name it yourself if that
matters to you.}
}
\value{
A \link{random_variable}.
}
\description{
For a mixing measure W, \eqn{X(x) = P_W(x)}{X(x) = P_W(x)}. A likelihood
ratio can be defines as a quotient of two such random variables.
}
\examples{
fam <- gaussian_family(dim = 2)
Q <- mixture_likelihood(mixture(point_mixing(c(0.5, 0.5)), fam))
Q(c(2,2))
}
\seealso{
\link{random_variable_arithmetic}
}
