% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{resample_engine}
\alias{resample_engine}
\title{A fresh, independent engine over a newly drawn sample from Q}
\usage{
resample_engine(engine, n_draws)
}
\arguments{
\item{engine}{An \code{expectation_engine}.}

\item{n_draws}{Draws for the fresh sample (ignored by deterministic engines).}
}
\value{
An \code{expectation_engine} of the same class as \code{engine}.
}
\description{
Returns an engine of the same kind that integrates against a fresh realisation
of Q. Deterministic (enumerating) engines integrate the true Q exactly, so
there is nothing to resample and the engine is returned unchanged. Monte Carlo
engines draw a new \code{n_draws} sample, independent of the one used to fit \verb{P*} --
the honest basis for certifying a mixture that was optimised against a
different sample. Pair it with \code{\link[=certify]{certify()}} to re-certify a fitted projection
on a fresh or larger sample, e.g.
\code{certify(res$projection, problem, engine = resample_engine(problem$engine, 1e6))}.
}
