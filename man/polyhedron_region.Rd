% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/region.R
\name{polyhedron_region}
\alias{polyhedron_region}
\alias{format,ripr::polyhedron_region-method}
\alias{print,ripr::polyhedron_region-method}
\title{A convex polyhedron given by its generators}
\usage{
polyhedron_region(vertices = NULL, rays = NULL, lines = NULL, facets = NULL)
}
\arguments{
\item{vertices}{\verb{(d, nv)} numeric matrix, one vertex per column, or \code{NULL}
for a cone anchored at the origin.}

\item{rays}{\verb{(d, nr)} numeric matrix of recession directions, or \code{NULL}.}

\item{lines}{\verb{(d, nl)} numeric matrix spanning the lineality space, or
\code{NULL}.}

\item{facets}{Optionally, the half-space description already known to the
caller: a list with \code{a} (\verb{(m, d)} matrix), \code{b} (length \code{m}) and \code{eq}
(logical, length \code{m}), read as \code{a \%*\% theta <= b} with \code{eq} flagging
equality rows.}
}
\value{
A \code{polyhedron_region}.
}
\description{
The concrete base of every convex region in the package: the Minkowski--Weul
form \eqn{\mathrm{conv}(V) + \mathrm{cone}(R) + \mathrm{span}(L)}{conv(V) + cone(R) + span(L)}.
\code{\link[=polytope_region]{polytope_region()}}, \code{\link[=simplex_region]{simplex_region()}}, \code{\link[=halfspace_region]{halfspace_region()}},
\code{\link[=point_region]{point_region()}} and \code{\link[=unconstrained_region]{unconstrained_region()}} are all special cases that
have friendlier constructors and do extra validation; use this one when
you know the generators themselves, or \code{\link[=h_region]{h_region()}} if you have the
H-representation.

\code{print()} summarises the geometry -- the generator blocks, the
facet counts, and for a small polytope the vertices themselves -- rather
than dumping the properties.
}
\details{
The \code{\link[=chart]{chart()}} is based directly on the generators:
\deqn{\theta(u) = V\,\mathrm{softmax0}(u_v) + L u_l + R\,\mathrm{softplus}(u_r)}{
theta(u) = V softmax0(u_v) + L u_l + R softplus(u_r)}
with \code{n_par = (nv - 1) + nl + nr} coordinates ordered \verb{(u_v, u_l, u_r)}:
vertex weights, then lineality, then rays. That every polyhedron admits
such a generator form, dually to its H-representation, is the Minkowski--Weyl
theorem (Ziegler 1995, Theorem 1.2).
}
\section{Properties}{

\describe{
\item{\code{generators}}{\code{list(v, r, l)} of numeric matrices, one generator
per column.}
\item{\code{facets}}{The half-space description as above, or \code{NULL} when it has
not been derived.}
\item{\code{q_cache}}{The exact rational representations, set at
construction: for a declared region the exact rationals of its doubles,
and for a region the set algebra produced, the exact cell itself -- so
further algebra never starts from a rounding. \code{h} is \code{NULL} only above
the facet guard.}
}
}

\examples{
# The halfspace `{theta_1 <= 0}` in R^2, by hand:
polyhedron_region(
  vertices = matrix(c(0, 0), ncol = 1),
  rays = matrix(c(-1, 0), ncol = 1),
  lines = matrix(c(0, 1), ncol = 1)
)
}
\references{
\insertRef{Ziegler1995}{ripr}

\insertRef{BeckTeboulle2009}{ripr}

\insertRef{DuchiShalevShwartz2008}{ripr}

\insertRef{ODonoghueCandes2015}{ripr}
}
