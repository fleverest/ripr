% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/steps.R
\name{oracle_step}
\alias{oracle_step}
\title{Frank-Wolfe oracle sweep over every null face}
\usage{
oracle_step(state, problem, n_seeds)
}
\arguments{
\item{state}{A \code{mixture_state}.}

\item{problem}{Problem list from \code{\link[=ripr_problem]{ripr_problem()}}.}

\item{n_seeds}{Random Dirichlet seeds per face.}
}
\value{
List with \code{E_star}, \code{best_theta}, \code{best_fi}, \code{se}, \code{face_results},
\code{log_Pw}.
}
\description{
Finds \verb{argmax_theta G(theta) = E_Q[p_theta / P_W]} over the union of faces at
the current state. Does not modify the state. The returned \code{log_Pw} is the
mixture log-density the sweep was run against; downstream weight-update steps
reuse it so the whole iteration sees one consistent P_W.
}
\keyword{internal}
