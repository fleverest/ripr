% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/e_variable.R
\name{e_variable}
\alias{e_variable}
\title{RIPr e-variable}
\usage{
e_variable(numerator, projection, gap = 0)
}
\arguments{
\item{numerator}{The numerator \code{Q} as an \link{outcome_distribution}.}

\item{projection}{The reverse information projection \verb{P*} as an
\link{outcome_distribution} (typically a \link{marginal} whose mixing measure is a
\link{finite_mixing}).}

\item{gap}{The certified duality gap used to rescale. Default \code{0}. Floored at
\code{0}, since a negative gap is numerical noise and would otherwise inflate the
e-value.}
}
\value{
An \code{e_variable}.
}
\description{
Bundles a numerator distribution \code{Q} and its reverse information projection
\verb{P*} into a single object that evaluates the e-variable on data. The rescaled
e-variable \verb{(Q / P*) / (1 + gap)} has expectation at most 1 under every null
distribution; \code{gap} is the certified duality gap the projection achieved
(\code{0} leaves the raw likelihood ratio uncorrected).
}
\details{
Usually obtained by fitting with \code{\link[=run_ripr]{run_ripr()}} (as the \verb{$e_variable} element of
its result), but can also be assembled directly from the pieces.
}
