% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_space.R
\name{validate_outcome}
\alias{validate_outcome}
\title{Coerce and check elements of a sample space}
\usage{
validate_outcome(space, x)
}
\arguments{
\item{space}{A \link{sample_space}.}

\item{x}{A length-\code{d} vector or \verb{(n, d)} matrix.}
}
\value{
\verb{(n, d)} numeric matrix.
}
\description{
Accepts one element as a length-\code{d} vector or \code{n} of them as an \verb{(n, d)}
matrix, and returns the \verb{(n, d)} form. Anything else is an error: a random
variable is only defined on its own sample space.
}
\examples{
validate_outcome(count_space(n = 4L, k = 3L), c(2L, 1L, 1L))
}
