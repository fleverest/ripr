% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quadrature.R
\name{expect_se}
\alias{expect_se}
\title{Standard error of an expectation}
\usage{
expect_se(engine, v)
}
\arguments{
\item{engine}{A \link{quadrature}.}

\item{v}{Length-\code{M} numeric vector of integrand values at the nodes.}
}
\value{
Numeric scalar.
}
\description{
The Monte Carlo standard error of \code{\link[=expect_q]{expect_q()}}, and exactly \code{0} for a
deterministic rule. A deterministic rule with approximation error has bias
rather than variance, so \code{0} here is not a claim of exactness.
}
\details{
Nothing in the fit calls this. It is here for a fit loop or a diagnostic of
your own, to judge whether a Monte Carlo engine carries enough draws.
}
\examples{
set.seed(1)
fam <- multinomial_family(n_trials = 3L, k = 2L)
Q <- induced_distribution(fam, dirac(c(0.5, 0.5)))
engine <- resolve_engine(mc_engine(200L), Q, fam)
expect_se(engine, engine@nodes[, 1])
}
