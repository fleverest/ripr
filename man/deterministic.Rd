% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quadrature.R
\name{deterministic}
\alias{deterministic}
\title{Is this rule free of sampling error?}
\usage{
deterministic(engine)
}
\arguments{
\item{engine}{A \link{quadrature}.}
}
\value{
\code{TRUE} or \code{FALSE}.
}
\description{
\code{TRUE} for exact enumeration and deterministic quadrature, \code{FALSE} for Monte
Carlo. A deterministic rule may still carry approximation error, but it is
bias rather than variance, so \code{\link[=expect_se]{expect_se()}} does not describe it.
}
\examples{
fam <- multinomial_family(n_trials = 3L, k = 2L)
Q <- induced_distribution(fam, point_mixing(c(0.5, 0.5)))
deterministic(resolve_engine(exact_engine(), Q, fam))
deterministic(resolve_engine(mc_engine(100L), Q, fam))
}
