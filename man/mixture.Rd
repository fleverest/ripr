% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixture.R
\name{mixture}
\alias{mixture}
\title{Mixture \code{P_W} induced by a mixing measure and a family}
\usage{
mixture(mixing = mixing_measure(), family = parametric_family())
}
\arguments{
\item{mixing}{A \link{mixing_measure} over the parameter space.}

\item{family}{The \link{parametric_family} whose kernel is mixed.}
}
\value{
A \code{mixture}.
}
\description{
The law of \code{X} when \eqn{\theta \sim W}{theta ~ W} and
\eqn{X \sim p_\theta}{X ~ p_theta}. Unlike a bare \link{mixing_measure} it carries
its family, so it is evaluable on data. Both \eqn{Q = P_{W_1}}{Q = P_W1} and
\eqn{\widehat{P}^* = P_{\widehat{W}_0}}{P_star_hat} are mixtures.
}
\examples{
fam <- multinomial_family(n_trials = 4L, k = 3L)
mixture(point_mixing(c(0.5, 0.3, 0.2)), fam)
}
