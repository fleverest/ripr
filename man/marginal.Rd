% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marginal.R
\name{marginal}
\alias{marginal}
\title{Outcome distribution induced by mixing a family}
\usage{
marginal(mixing = mixing(), family = family())
}
\arguments{
\item{mixing}{A \link{mixing} measure over the parameter space.}

\item{family}{The sampling \link{family} whose kernel \code{p_theta} is mixed.}
}
\value{
A \code{marginal}.
}
\description{
A \code{marginal} is a self-contained \link{outcome_distribution}: the law of \code{X} when
\code{theta} is drawn from a \link{mixing} measure and \code{X ~ p_theta}, i.e.
\verb{m(x) = integral p_theta(x) d(mixing)(theta)}. Unlike a bare \code{mixing}, it
carries its family, so \code{\link[=dist_log_density]{dist_log_density()}} and \code{\link[=dist_sample]{dist_sample()}} need no
separate family argument. This is the object the alternative Q and the
projection P* become when evaluated on data (see \code{\link[=e_variable]{e_variable()}}).
}
\details{
Build one with \code{\link[=as_marginal]{as_marginal()}} rather than the raw constructor.
}
