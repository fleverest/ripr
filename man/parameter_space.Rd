% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parameter_space.R
\name{parameter_space}
\alias{parameter_space}
\title{Parameter spaces}
\usage{
parameter_space()
}
\description{
A \code{parameter_space} is a convex set of parameter values. The same type serves
two roles. A family's own \eqn{\Theta}{Theta} is one, and so is each convex
piece \eqn{\Theta_{0i}}{Theta_0i} of a null hypothesis
\eqn{\Theta_0 = \bigcup_i \Theta_{0i}}{Theta_0 = union_i Theta_0i}. A null's
pieces may overlap.
}
\details{
A \code{parameter_space} object encodes the geometry: it encodes dimension,
membership checking, projection, and a \code{\link[=chart]{chart()}} that maps unconstrained
coordinates to the space. The oracle is written once against the chart, so
a new geometry needs no optimisation code.

The contrast with \link{sample_space} is that these are searched over: outcomes
only ever need validating, whereas parameters need coordinates for a
optimiser to search over, which is what \code{\link[=chart]{chart()}} supplies. There may be
null geometries that do not permit a \code{\link[=chart]{chart()}}, but these are currently
beyond the scope of this package.
}
\examples{
# `parameter_space` is abstract; simplex_region(), halfspace_region(),
# point_region() and real_region() subclass it, e.g.
s <- simplex_region(vertices = diag(3))
S7::S7_inherits(s, parameter_space)
space_dim(s)
}
