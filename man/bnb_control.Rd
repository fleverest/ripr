% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/oracle_bound.R
\name{bnb_control}
\alias{bnb_control}
\title{Tuning for the Bernstein branch-and-bound bound}
\usage{
bnb_control(
  tol = 0.001,
  max_iter = 500L,
  max_coef = 2e+05,
  keep_argmax = FALSE,
  round_slack = TRUE
)
}
\arguments{
\item{tol}{Stop subdividing once \code{bound - incumbent <= tol}. The bound is
valid at every iteration, so this trades tightness for time, never
validity.}

\item{max_iter}{Cap on bisections per face. Reaching it is not an error: the
bound returned is still valid, merely looser.}

\item{max_coef}{Refuse to certify problems with more Bernstein coefficients
than this, so an oversized batch is a planned fallback to the heuristic
oracle rather than an out-of-memory kill. Coefficients per sub-simplex are
\code{choose(n + K - 1, K - 1)}: 10,626 at \verb{n = 20, K = 5}; 4.6M at
\verb{n = 100, K = 5}. Certification caps the usable batch size more tightly
than optimisation does.}

\item{keep_argmax}{Prune with \code{U(S) < incumbent} rather than
\code{U(S) <= incumbent}, retaining ties, so that the returned \code{active} set is a
certified enclosure of every maximiser. Costs a little work and some
memory; \code{active} is \code{NULL} without it.}

\item{round_slack}{Add \code{n * .Machine$double.eps * max(abs(coef))} to the
reported bound. de Casteljau is all convex combinations and so is stable,
but round-to-nearest can put the computed maximum coefficient marginally
\emph{below} the true one, which is the unsafe direction for a validity claim.}
}
\value{
A named list of the above.
}
\description{
Tuning for the Bernstein branch-and-bound bound
}
