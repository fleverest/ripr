% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_space.R
\name{sample_space}
\alias{sample_space}
\title{Sample spaces}
\usage{
sample_space()
}
\description{
A \code{sample_space} defines the set \eqn{\mathcal{X}}{X} that observation are
drawn from, and nothing about how it is drawn. It answers three questions:
how many coordinates one element has (\code{\link[=space_dim]{space_dim()}}), whether a given object
is an element (\code{\link[=validate_outcome]{validate_outcome()}}) and, when the space is finite,
(\code{\link[=is_finite_space]{is_finite_space()}}), what all of the possible outcomes are
(\code{\link[=enumerate_space]{enumerate_space()}}).
}
\details{
This is deliberately separate from \link{parametric_family}. The role that
sample spaces take in this package is effectively just defining the outcome
space for a distribution, and the object is in charge of validation and
carrying data needed upstream.
}
\examples{
# `sample_space` is abstract; spaces subclass it, e.g.
S7::S7_inherits(count_space(n = 4L, k = 3L), sample_space)
}
