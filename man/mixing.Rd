% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixing.R
\name{mixing}
\alias{mixing}
\title{Mixing measure over a family's parameter space}
\usage{
mixing()
}
\description{
A \code{mixing} is the parameter-space branch of \link{distribution}: a law over the
parameter set Theta of a sampling \code{\link[=family]{family()}} -- a point mass (\code{\link[=point_mixing]{point_mixing()}}),
a finite set of weighted atoms (\code{\link[=finite_mixing]{finite_mixing()}}), or a continuous prior
(e.g. \code{\link[=gaussian_mixing]{gaussian_mixing()}}). On its own it is a law over \emph{parameters} and knows
nothing of a sample space; paired with a family via \code{\link[=as_marginal]{as_marginal()}} it induces
a \link{marginal} over \emph{outcomes} -- the role it plays as the alternative Q and as
the fitted reverse information projection P*. A \code{mixing} knows nothing about
nulls, projections, or algorithms.
}
