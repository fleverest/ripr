% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/faces.R
\name{polytope_face}
\alias{polytope_face}
\title{Convex polytope face given by a vertex matrix}
\usage{
polytope_face(vertices, face_index, init_point_fn = NULL, pinv = NULL)
}
\arguments{
\item{vertices}{\verb{(d, V)} numeric matrix; columns are the polytope vertices.}

\item{face_index}{Integer label carried through results.}

\item{init_point_fn}{Optional function of a reference point returning an
initial atom on the face; defaults to \code{\link[=project]{project()}}.}

\item{pinv}{Optional precomputed \verb{(V, d)} left pseudo-inverse of \code{vertices}.}
}
\value{
A \code{polytope_face}.
}
\description{
The workhorse face type: a bounded convex polytope described by the columns
of \code{vertices}, parametrised by convex combinations. The vertex matrix is the
constant Jacobian, and the SVD-based left pseudo-inverse (computed once at
construction unless supplied) drives the cheap coordinate recovery.
}
\details{
The oracle is a softmax + BFGS search with Dirichlet random restarts:
\code{n_seeds} uniform-Dirichlet points are scored with \code{objective$value_batch},
the best \code{n_restarts} seed BFGS runs in the unconstrained softmax
parametrisation, and the best BFGS result wins. Supplying \code{seed_alpha} skips
the global search (single local refinement, as used by the EM M-step).
}
