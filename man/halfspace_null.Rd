% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/null.R
\name{halfspace_null}
\alias{halfspace_null}
\title{Halfspace given by a normal and an offset}
\usage{
halfspace_null(normal, offset = 0)
}
\arguments{
\item{normal}{Normal vector \code{a}; must be non-zero.}

\item{offset}{Offset \code{b}.}
}
\value{
A \code{halfspace_null}.
}
\description{
The unbounded case:
\eqn{\{\theta : a^\top \theta \le b\}}{{theta : a'theta <= b}}
(H-representation). The plurality subnull
\eqn{\{\theta : \theta_1 \le \theta_j\}}{{theta : theta_1 <= theta_j}} is
\code{normal = e_1 - e_j}, \code{offset = 0}.
}
\details{
Coordinates are \verb{(z, s)}: \code{z} positions a point on the bounding hyperplane in
an orthonormal basis, and \code{softplus(s) >= 0} is the distance inward.

Having no vertices, a halfspace admits no certified gap bound. That is a
property of the representation rather than a missing feature -- see
\code{\link[=simplex_null]{simplex_null()}} for the bounded alternative.
}
\examples{
# `{theta : theta_1 <= theta_2}`
halfspace_null(normal = c(1, -1), offset = 0)
}
