% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/steps.R
\name{mirror_descent_weights}
\alias{mirror_descent_weights}
\title{Fully-corrective weight reoptimisation via entropic mirror descent}
\usage{
mirror_descent_weights(
  loss_and_grad,
  weights,
  max_iters,
  atol = 1e-12,
  rtol = 1e-08
)
}
\arguments{
\item{loss_and_grad}{Function of \code{w} returning \code{list(loss = , grad = )}.}

\item{weights}{Initial weight vector.}

\item{max_iters}{Iteration cap.}

\item{atol, rtol}{Convergence tolerances.}
}
\value{
\code{list(weights = , loss = )}.
}
\description{
Minimises the KL loss over the weight simplex with the atoms held fixed,
using exponentiated-gradient updates \code{w <- w * exp(-eta * grad)} (normalised)
with backtracking on the step size eta. \code{loss_and_grad(w)} must return
\code{list(loss = , grad = )}; convergence is declared when the per-iteration
decrease falls below \code{atol + rtol * abs(loss)}.
}
\keyword{internal}
