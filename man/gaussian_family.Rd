% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gaussian.R
\name{gaussian_family}
\alias{gaussian_family}
\title{Gaussian sampling family with known covariance}
\usage{
gaussian_family(dim, sigma = NULL)
}
\arguments{
\item{dim}{Integer dimension of the observation.}

\item{sigma}{Known covariance matrix, or \code{NULL} for the identity.}
}
\value{
A \code{gaussian_family}.
}
\description{
Observations are single draws \code{X ~ N(theta, sigma)} with \code{sigma} known. The
covariance is Cholesky-whitened once at construction, so densities and scores
cost one triangular solve; with the default \code{sigma = NULL} (identity) the
family is already white.
}
\details{
The sample space is continuous, so \code{\link[=support]{support()}} signals an error: pair this
family with a Monte Carlo engine.
}
