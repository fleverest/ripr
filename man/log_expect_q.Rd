% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quadrature.R
\name{log_expect_q}
\alias{log_expect_q}
\title{Expectation under the alternative, in log space}
\usage{
log_expect_q(engine, log_v)
}
\arguments{
\item{engine}{A \link{quadrature}.}

\item{log_v}{Length-\code{M} numeric vector of log integrand values at the nodes.}
}
\value{
Numeric scalar.
}
\description{
\eqn{\log E_Q[v]}{log E_Q[v]} where \code{v >= 0} is supplied as \code{log_v}. Stable
where \code{\link[=expect_q]{expect_q()}} would overflow, which it does routinely: likelihood ratios
at audit scale exceed the range of a double long before they stop being
meaningful.
}
\details{
You need this only if you are writing a fit loop or an engine spec of your
own; in normal use the step verbs and \code{\link[=ripr_finish]{ripr_finish()}} take the expectations.
}
\examples{
fam <- multinomial_family(n_trials = 3L, k = 2L)
Q <- induced_distribution(fam, point_mixing(c(0.5, 0.5)))
engine <- resolve_engine(exact_engine(), Q, fam)
log_expect_q(engine, log(engine@nodes[, 1] + 1))
}
