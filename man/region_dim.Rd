% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/region.R, R/region_algebra.R
\name{region_dim}
\alias{region_dim}
\alias{region_dim,ripr::empty_region-method}
\alias{region_dim,ripr::union_region-method}
\title{The dimension of a region's affine hull}
\usage{
region_dim(space)
}
\arguments{
\item{space}{A \link{region}.}
}
\value{
An integer between -1 and \code{space_dim(space)}.
}
\description{
An \code{\link[=empty_region]{empty_region()}} has dimension -1, a point has dimension 0, a segment 1,
and so on.

The empty region has affine dimension -1: strictly below a
point, which is a region with a member.

A union's affine dimension is the largest among its parts:
a lower-dimensional part adds nothing to the hull of the largest.
}
\details{
Contrast \code{\link[=space_dim]{space_dim()}}, which is the number of coordinates a point of the
region carries. That ambient dimension belongs to the family's parameter
space; the affine dimension belongs to the region's own geometry. It is
most the ambient dimension, but may be lower when the region imposes extra
constraints (e.g. a lower-dimension null may set theta1 = theta2).
}
\examples{
region_dim(point_region(theta = c(0.5, 0.3, 0.2)))
region_dim(simplex_region(vertices = diag(3)))
region_dim(real_region(3L))
}
