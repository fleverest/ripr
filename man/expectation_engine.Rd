% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{expectation_engine}
\alias{expectation_engine}
\title{Expectation engine: the seam between algorithms and the sample space}
\usage{
expectation_engine(
  family = NULL,
  alternative = NULL,
  id = character(0),
  M = integer(0),
  outcomes = NULL,
  log_q_mass = NULL,
  q_mass = NULL,
  finite_q = NULL,
  q_mass_f = NULL,
  M_f = integer(0),
  H_Q = integer(0)
)
}
\arguments{
\item{family, alternative}{The problem components the engine integrates.}

\item{id}{Character identifier for the engine.}

\item{M, M_f}{Outcome counts: all outcomes, and the positive-Q-mass subset.}

\item{outcomes}{The \verb{(M, d)} outcome set the engine integrates over.}

\item{log_q_mass, q_mass, q_mass_f}{Q-mass over the outcome set: log, linear,
and the positive-mass subset.}

\item{finite_q}{Logical mask of the positive-Q-mass outcomes.}

\item{H_Q}{Shannon entropy of Q.}
}
\description{
An \code{expectation_engine} owns everything needed to take expectations under the
alternative distribution Q: for finite sample spaces this is the full support
enumeration and the vector of Q-masses; for continuous spaces a fixed set of
Monte Carlo draws from Q (common random numbers). All downstream objective
quantities -- the Frank-Wolfe objective \code{G(theta) = E_Q[p_theta / p_W]}, its
gradients, the KL divergence, and the duality gap -- are written against the
generics below and never against a concrete engine.
}
\details{
Every engine stores a working set of \code{M} outcomes and a log-weight vector
\code{log_q_mass} such that \verb{E_Q[f] = sum_i exp(log_q_mass_i) f(outcome_i)}. Exact
engines enumerate the support with true Q-masses; Monte Carlo engines store
draws from Q with uniform weights \code{1/M}, making the same expressions the plain
Monte Carlo estimators. The generics accept base R vectors/matrices defined
over the engine's outcome rows, all in log space.

The properties are populated by the concrete engine constructors
(\code{\link[=exact_engine]{exact_engine()}}, \code{\link[=mc_engine]{mc_engine()}}); this abstract base is not constructed
directly.
}
