% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{lb_step}
\alias{lb_step}
\title{Li--Barron greedy step}
\usage{
lb_step(
  state,
  times = 1L,
  directions = "forward",
  size = c("line-search", "fixed"),
  correct = FALSE,
  record_gap = FALSE,
  until = NULL
)
}
\arguments{
\item{state}{A \link{ripr_state}.}

\item{times}{Steps to take.}

\item{directions}{Any of \code{"forward"}, \code{"pairwise"}, \code{"away"}. More than one
means each is tried and whichever reaches the lowest KL is taken.}

\item{size}{\code{"line-search"}, or \code{"fixed"} for the open-loop schedule.}

\item{correct}{Re-solve every weight inside each candidate evaluation, using
\code{lb_fc_tol} and \code{lb_fc_max_iter} from \code{\link[=ripr_control]{ripr_control()}}.}

\item{record_gap}{Also sweep the Frank--Wolfe oracle, purely to record a gap.
\code{FALSE} by default.}

\item{until}{Optional predicate; see \link{predicates}.}
}
\value{
The updated \link{ripr_state}.
}
\description{
Scores each candidate by the KL it yields \emph{after} the new weights are
chosen, so the step rule runs as an inner optimisation. Considerably more
expensive than \code{\link[=fw_step]{fw_step()}}; see \link{oracles}.
}
\seealso{
\link{oracles}, \link{predicates}
}
