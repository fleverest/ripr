% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/steps.R
\name{li_barron_step}
\alias{li_barron_step}
\title{Li-Barron greedy step: exact-objective oracle plus line search}
\usage{
li_barron_step(state, problem, n_seeds, ls_tol = 1e-12)
}
\arguments{
\item{state}{A \code{mixture_state}; mutated in place.}

\item{problem}{Problem list.}

\item{n_seeds}{Random Dirichlet seeds per face.}

\item{ls_tol}{Line-search tolerance.}
}
\value{
List with \code{eps_star}, \code{prop_star} (\code{NA}), \code{best_theta}, \code{best_fi},
and \code{value}.
}
\description{
Where the Frank-Wolfe oracle maximises the linearisation
\code{E_Q[p_theta / P_W]}, the Li-Barron step selects the atom maximising the
exact post-update objective \verb{max_eps E_Q[log((1 - eps) P_W + eps p_theta)]},
with the inner \code{eps} solved by the 1-D line search and the outer \code{theta} by
the face oracle. The oracle's gradient uses the envelope theorem (the inner
maximiser is held fixed), and its seed grid is ranked by the cheap linearised
objective before the exact objective drives BFGS refinement.
}
\details{
Runs its own oracle sweep, so schedulers pairing it with a duality-gap record
still need a separate \code{\link[=oracle_step]{oracle_step()}}.
}
\keyword{internal}
